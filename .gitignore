node_modules/
dist/
.vercel
.env*

# Local agent tooling (npx skills add, Claude Code settings) — not part of
# the application, personal to this machine.
.claude/
.agents/
skills-lock.json

# Last reading kept by scripts/vote-alerts.ts (CI keeps it in the Actions cache instead)
data/vote-alert-state.json
